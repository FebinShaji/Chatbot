#include <stdio.h>

//This function will print the regular menu to the user if they ask for it
void showRegMenu();

//This function will print the vegetarian menu to the user if they ask for it
void showVegMenu();

//This function will print the drinks menu to the user if they ask for it
void showDrinksMenu();

//This function shows the options for the menus to the user
//Then checks if the user chooses a valid menu option
//Depending on the menu option picked will execute one of the 3 oother functions thats display the menus
int menu();
